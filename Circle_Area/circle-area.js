function calcCircleArea(r) {
    let radius = r;
    let area = Math.PI * Math.pow(radius, 2);
    document.write("r = " + radius+ "; area= " + area + "</br>");
}
calcCircleArea(7);
calcCircleArea(1.5);
calcCircleArea(20);